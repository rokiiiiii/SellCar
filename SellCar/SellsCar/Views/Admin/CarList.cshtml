@model SellCar.Domain.ViewModels.Cars.CarsListViewModel
@{
    Layout = "_LayoutAdmin";
}
<div class="container-fluid my-2">
    <h3 class="mt-1">Cars brands</h3>
    <hr class="my-3">
    <div class="row">
        <div class="col-md-12">
            <p>
                <a class="btn btn-primary" data-toggle="collapse" href="#collapseExample" role="button" aria-expanded="false" aria-controls="collapseExample">
                    Add new car
                </a>
            </p>
            <div class="collapse" id="collapseExample">
                <div class="card card-body">
                   
                </div>
            </div>
            <table id="example" class="table table-striped table-bordered dt-responsive " style="width:100%">
                <thead>
                    <tr>
                        <th>Car Id</th>
                        <th>Car name</th>
                        <th>Car Url</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Car)
                    {
                        <tr>
                            <td>@item.CarId</td>
                            <td>@item.Name</td>
                            <td>@item.Url</td>
                            <td>
                                <a href="/admin/car/@item.CarId" class="btn btn-primary btn-sm mr-2"><i class="fas fa-edit"></i></a>
                                <form action="/admin/car/delete" method="post" style="display:inline">
                                    <input type="hidden" name="MarkaId" value="@item.CarId" />
                                    <button type="submit" class="btn btn-danger btn-sm "><i class="fas fa-trash-alt"></i></button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
